[package]
name = "simulate"
version = "0.1.0"
edition = "2024"

[dependencies]
llmsub = { path = "../../libraries/rust/llmsub" }
regex = "1.10"
clap = { version = "4.0", features = ["derive"] }

[dev-dependencies]
tempfile = "3.8"
