# Scan this file for changes every 30 seconds
refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console

root:
  level: debug
  appenders:
    - stdout

loggers:
  tailborn:
    level: debug
    appenders:
      - stdout
    additive: true
